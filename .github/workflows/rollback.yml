# Rollback — manual dispatch. Picks a previous SHA/tag, re-renders .env.local
# from the SAME environment secrets, and redeploys that commit via ci-deploy.sh.
# Migrations are NOT reversed (prisma migrate deploy is forward-only).
#
# Runs on the SELF-HOSTED runner. No SSH — everything is local on the box.
#
# WARNING: If a forward migration ran between the target ref and HEAD,
# rolling back the CODE without rolling back the DB may break the app.
# Inspect packages/db/prisma/migrations/ between target ref and HEAD first.
name: Rollback

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Which environment to roll back?"
        type: choice
        options: [production, development]
        required: true
        default: production
      ref:
        description: "Commit SHA, tag, or branch to roll back to"
        type: string
        required: true
      reason:
        description: "Why? (recorded in audit log)"
        type: string
        required: true

concurrency:
  group: rollback-${{ github.event.inputs.environment }}
  cancel-in-progress: false

permissions:
  contents: read
  deployments: write

jobs:
  rollback:
    name: Rollback ${{ github.event.inputs.environment }} → ${{ github.event.inputs.ref }}
    runs-on: [self-hosted, LXC]
    environment: ${{ github.event.inputs.environment }}
    steps:
      - name: Resolve the target ref to a concrete SHA
        id: resolve
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TARGET_REF: ${{ github.event.inputs.ref }}
        run: |
          SHA="$(gh api repos/${{ github.repository }}/commits/"$TARGET_REF" --jq '.sha')"
          [ -z "$SHA" ] && { echo "ERROR: could not resolve ref '$TARGET_REF'" >&2; exit 1; }
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"
          echo "Resolved $TARGET_REF → $SHA"
          echo "::notice::Rollback ${{ github.event.inputs.environment }} to $SHA. Reason: ${{ github.event.inputs.reason }}"

      - name: Start GitHub deployment record
        uses: bobheadxi/deployments@v1
        id: deployment
        with:
          step: start
          token: ${{ secrets.GITHUB_TOKEN }}
          env: ${{ github.event.inputs.environment }}
          ref: ${{ steps.resolve.outputs.sha }}
          desc: "ROLLBACK to ${{ github.event.inputs.ref }} — ${{ github.event.inputs.reason }}"

      - name: Render .env.local + run deploy
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
          NEXTAUTH_SALT: ${{ secrets.NEXTAUTH_SALT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_API_URL: ${{ secrets.OPENAI_API_URL }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          MCP_AUTH_TOKEN: ${{ secrets.MCP_AUTH_TOKEN }}
          WOMPI_ENV: ${{ secrets.WOMPI_ENV }}
          WOMPI_PUBLIC_KEY: ${{ secrets.WOMPI_PUBLIC_KEY }}
          WOMPI_EVENTS_SECRET: ${{ secrets.WOMPI_EVENTS_SECRET }}
          WOMPI_INTEGRITY_SECRET: ${{ secrets.WOMPI_INTEGRITY_SECRET }}
          ENV_NAME: ${{ github.event.inputs.environment }}
          TARGET_SHA: ${{ steps.resolve.outputs.sha }}
          DEPLOY_ACTOR: ${{ github.actor }}
        run: |
          case "$ENV_NAME" in
            production)  APP_DIR="/opt/trt";     URL="https://my-testo.com";     DATA_DIR="/var/lib/trt";     PORT="8002" ;;
            development) APP_DIR="/opt/trt-dev"; URL="https://dev.my-testo.com"; DATA_DIR="/var/lib/trt-dev"; PORT="8003" ;;
          esac
          umask 077
          cat > "$APP_DIR/apps/web/.env.local" <<EOF
          # AUTO-RENDERED by Rollback at ${TARGET_SHA}
          # REASON: ${{ github.event.inputs.reason }}
          DATABASE_URL="${DATABASE_URL}"
          NEXTAUTH_URL="${URL}"
          AUTH_SECRET="${AUTH_SECRET}"
          NEXTAUTH_SALT="${NEXTAUTH_SALT}"
          OPENAI_API_KEY="${OPENAI_API_KEY}"
          OPENAI_API_URL="${OPENAI_API_URL}"
          OPENAI_MODEL="${OPENAI_MODEL}"
          RESEND_API_KEY="${RESEND_API_KEY}"
          EMAIL_FROM="${EMAIL_FROM}"
          GOOGLE_CLIENT_ID="${GOOGLE_CLIENT_ID}"
          GOOGLE_CLIENT_SECRET="${GOOGLE_CLIENT_SECRET}"
          MCP_AUTH_TOKEN="${MCP_AUTH_TOKEN}"
          WOMPI_ENV="${WOMPI_ENV}"
          WOMPI_PUBLIC_KEY="${WOMPI_PUBLIC_KEY}"
          WOMPI_EVENTS_SECRET="${WOMPI_EVENTS_SECRET}"
          WOMPI_INTEGRITY_SECRET="${WOMPI_INTEGRITY_SECRET}"
          UPLOADS_DIR="${DATA_DIR}/uploads"
          KB_DB_PATH="${DATA_DIR}/kb/knowledge.db"
          KB_TEXT_DIR="${DATA_DIR}/kb/text"
          CORPUS_DIR="${DATA_DIR}/corpus"
          OCR_ENDPOINT="https://imagedit.powerhousegym.co/ocr"
          GRAPHITI_MCP_URL="http://127.0.0.1:8000"
          GRAPH_QUERY_URL="http://127.0.0.1:8001"
          MCP_HTTP_HOST="127.0.0.1"
          MCP_HTTP_PORT="${PORT}"
          NODE_ENV="$ENV_NAME"
          EOF
          chmod 600 "$APP_DIR/apps/web/.env.local"

          # Run ci-deploy.sh from the right checkout
          cd "$APP_DIR"
          TARGET_SHA="$TARGET_SHA" DEPLOY_ACTOR="$DEPLOY_ACTOR" \
            bash scripts/ci-deploy.sh "$ENV_NAME"

      - name: Finish deployment (success)
        if: success()
        uses: bobheadxi/deployments@v1
        with:
          step: finish
          token: ${{ secrets.GITHUB_TOKEN }}
          status: success
          env: ${{ github.event.inputs.environment }}
          deployment_id: ${{ steps.deployment.outputs.deployment_id }}

      - name: Finish deployment (failure)
        if: failure()
        uses: bobheadxi/deployments@v1
        with:
          step: finish
          token: ${{ secrets.GITHUB_TOKEN }}
          status: failure
          env: ${{ github.event.inputs.environment }}
          deployment_id: ${{ steps.deployment.outputs.deployment_id }}
