{
  "name": "@trt/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "MCP server exposing the TRT knowledge stack (deterministic KB + Graphiti graph + platform docs) to any MCP-capable AI model. Retrieval-only — see GOLD §2.",
  "scripts": {
    "start": "tsx src/stdio.ts",
    "start:http": "tsx src/http.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@trt/engine": "workspace:*",
    "@trt/kb": "workspace:*",
    "tsx": "^4.19.2",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
